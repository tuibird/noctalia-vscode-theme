Sourcecode for a Noctalia VS Code theme that Matugen can hook into. This is based heavily on the Hyprluna equivilent project. 

The asociated compiled vsix fil can be manually installed via the built in VS Code command ```
code --install-extension <noctalia location>/noctaliatheme-0.0.1.vsix```
